# Mapbox public token used by the service area map.
# Set the real value in your Cloudflare Pages environment variables.
# Note: this is a publishable token; never commit a secret token here.
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here